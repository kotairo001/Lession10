let inputWidth=parseInt(prompt("Input the rectagle's width!"));
let inputHeight=parseInt(prompt("Input the rectagle's height!"));
let area = inputWidth*inputHeight;
document.write("Rectagle's area is " + area);